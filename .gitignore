# Go build
*.exe
*.test
*.out
bin/
dist/
coverage.out
coverage-full.out
/vpc

# Newman snapshots and run logs (audit trail, локально, не commit)
tests/newman/out/snap*-local-*.json
tests/newman/out/snap*-local-run.log
tests/newman/out/baseline-*.json

# IDE
.idea/
.vscode/
.DS_Store
/migrator

# Vendored corelib-owned инфра-proto — единственный источник в kacho-corelib/proto.
# В git здесь НЕ хранится; `make proto-vendor` подтягивает перед buf generate/lint.
/proto/google/
/proto/kacho/cloud/api/operation.proto
/proto/kacho/cloud/operation/operation.proto
/proto/kacho/cloud/validation.proto
/proto/kacho/iam/authz/v1/authz_options.proto
go.work
go.work.sum

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/
