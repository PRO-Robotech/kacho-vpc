# Copyright (c) PRO-Robotech
# SPDX-License-Identifier: BUSL-1.1

# docker-build — multi-arch image build & push.
#
# Назначение: собирает Docker-образ kacho-vpc под linux/amd64 + linux/arm64 и
# пушит multi-arch manifest в реестр. Дополняет (НЕ заменяет) `ci.yaml`.
#
# ─── Почему ОДНА job, а не per-arch + manifest-combine ───────────────────
# Сборка манифеста через `docker manifest create`/`imagetools create` ОБЯЗАНА
# перечитать (pull) source-манифесты из реестра — это упирается в pull-лимит
# реестра и падает с `toomanyrequests` даже при зеленых per-arch билдах.
#
# Решение: ОДИН `buildx build --platform linux/amd64,linux/arm64 --push` —
# собирает обе платформы локально в buildkit и пушит ОДИН index напрямую.
# НИ ОДНОГО pull из основного реестра: base-образы, buildkit/binfmt — с
# mirror.gcr.io, а `push` против pull-лимита не считается. Dockerfile —
# cross-compile-aware (`FROM --platform=$BUILDPLATFORM golang` + `GOARCH=
# $TARGETARCH go build`), поэтому Go-компиляция идет нативно на amd64 и
# кросс-компилит arm64 (быстро); под QEMU эмулируется только крошечный
# финальный alpine-слой (`apk add ca-certificates`).
#
# ─── Требуемые GitHub secrets ────────────────────────────────────────────
# DOCKERHUB_USERNAME — registry username (он же namespace для образов).
# DOCKERHUB_TOKEN    — registry access token (Read/Write/Delete scope).
#
# Tags: <branch>-<sha8> (multiarch index).

name: docker-build

on:
  push:
    branches:
      - main
      - "KAC-*"
      - "feat/*"
      - "fix/*"
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+rc[0-9]+"

permissions:
  contents: read

env:
  IMAGE_NAME: kacho-vpc

jobs:
  docker-build:
    name: build & push multiarch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { path: kacho-vpc }

      - uses: benjlevesque/short-sha@v3.0
        id: short-sha
        with: { length: 8 }

      - name: Create sanitized branch name
        id: sanitize
        run: echo "branch=$(echo "${GITHUB_REF_NAME}" | sed 's/\//-/g')" >> "$GITHUB_OUTPUT"

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Enable QEMU emulation
        uses: docker/setup-qemu-action@v4
        with:
          # binfmt from a mirror — keeps the DockerHub pull budget free.
          image: mirror.gcr.io/tonistiigi/binfmt:latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          # buildkit from a mirror, not docker.io.
          driver-opts: image=mirror.gcr.io/moby/buildkit:buildx-stable-1

      - name: Configure Buildx cache
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.ref_name }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build and push (multiarch)
        uses: docker/build-push-action@v7
        with:
          context: kacho-vpc
          file: ./kacho-vpc/Dockerfile
          push: true
          provenance: false
          platforms: linux/amd64,linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ steps.sanitize.outputs.branch }}-${{ steps.short-sha.outputs.sha }}
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      - name: Move new cache
        if: always()
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache 2>/dev/null || true
